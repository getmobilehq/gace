
  # Setup React Application

  This is a code bundle for Setup React Application. The original project is available at https://www.figma.com/design/BTy55OFQ2pDJa5tUAkNEK0/Setup-React-Application.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  